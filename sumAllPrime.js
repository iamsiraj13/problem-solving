/**
 *
 * Sum all the prime numbers up to and including the
 * provided number
 *
 * @example
 * sumAllPrime(10) return 17
 * sumAllPrime(977) return 93156
 *
 */
function sumAllPrime(nums) {
  let primeTotal = 0;
  for (let i = 2; i < nums; i++) {
    console.log(nums[i]);
  }
}

sumAllPrime(977);
